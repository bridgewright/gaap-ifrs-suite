[project]
name = "gaap-ifrs"
version = "0.1.0"
description = "Local GAAP to K-IFRS conversion engine (evidence-grounded)"
requires-python = ">=3.11"
dependencies = ["openpyxl>=3.0"]

[project.scripts]
gaap-ifrs = "gaap_ifrs.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
